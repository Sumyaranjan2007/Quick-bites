{
  "about": "The versionCode of the last build. scripts/build-apks.sh takes the next one (one more than the higher of this and the build machine's own record), writes it into every app it builds, and records it here. Commit this file after a build. A build never reuses or lowers a code: Android refuses to install a lower one as an update, and an equal one hides which build is on a phone.",
  "lastVersionCode": 9,
  "lastBuiltAt": "2026-09-25"
}
